const rp = require('request-promise')

const url = require('./config.json').sensorthings.url + '/Sensors'

/*
let options1 = {
	url,
	body: {
		name: 'DHT22',
		description: 'NO',
		encodingType: 'application/pdf',
		metadata: '...pdf'
	},
	json: true
}
rp.post(options1)
	.then( body => {
		console.log(body)
	}).catch( error => {
		console.log(error)
	})
*/

/*
let options2 = {
	url,
	body: {
		name: 'PMS5003',
		description: 'NO',
		encodingType: 'application/pdf',
		metadata: '...pdf'
	},
	json: true
}
rp.post(options2)
	.then( body => {
		console.log(body)
	}).catch( error => {
		console.log(error)
	})
*/
