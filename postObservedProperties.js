const rp = require('request-promise')

const url = require('./config.json').sensorthings.url + '/ObservedProperties'

/*
let options1 = {
	url,
	body: {
		name: 'FAirKit Temperature',
		description: 'NO',
		definition: 'http://'
	},
	json: true
}
rp.post(options1)
	.then( body => {
		console.log(body)
	}).catch( error => {
		console.log(error)
	})
*/

/*
let options2 = {
	url,
	body: {
		name: 'FAirkit Humidity',
		description: 'NO',
		definition: 'http'
	},
	json: true
}
rp.post(options2)
	.then( body => {
		console.log(body)
	}).catch( error => {
		console.log(error)
	})
*/

/*
let options3 = {
	url,
	body: {
		name: 'FAirKit PM1.0',
		description: 'NO',
		definition: 'http://'
	},
	json: true
}
rp.post(options3)
	.then( body => {
		console.log(body)
	}).catch( error => {
		console.log(error)
	})
*/

/*
let options4 = {
	url,
	body: {
		name: 'FAirKit PM2.5',
		description: 'NO',
		definition: 'http://'
	},
	json: true
}
rp.post(options4)
	.then( body => {
		console.log(body)
	}).catch( error => {
		console.log(error)
	})
*/

/*
let options5 = {
	url,
	body: {
		name: 'FAirKit PM10',
		description: 'NO',
		definition: 'http://'
	},
	json: true
}
rp.post(options5)
	.then( body => {
		console.log(body)
	}).catch( error => {
		console.log(error)
	})
*/
